import { StyleSheet } from 'react-native';

import buttonsStyles from '../ButtonsCommonStyles';

const styles = StyleSheet.create({
    container: {
        ...buttonsStyles.container
    },

    label: {
        ...buttonsStyles.label
    }
});

export default styles;
